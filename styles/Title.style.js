import { StyleSheet } from "react-native";

const titleStyle = StyleSheet.create({
  txt: {
    fontSize: 24,
    color: "#022150",
    fontFamily: "Inter",
    fontWeight: "500",
  },
});
export default titleStyle;
